#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define FAST ios_base::sync_with_stdio(0), cin.tie(0), cout.tie(0);
#define nl '\n'

int main()
{
    FAST;
    int tc;
    cin >> tc;
    while (tc--)
    {
        ll l, r;
        cin >> l >> r;
        if (l == r && l % 2 != 0 && r % 2 != 0){

        }
        else
        {
            
        }
    }
    return 0;
}
